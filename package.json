{
  "devDependencies": {
    "@fontsource-variable/montserrat": "^5.1.1",
    "boxicons": "^2.1.4",
    "esbuild": "0.21.5"
  },
  "scripts": {
    "dev": "esbuild recursos/js/ingreso.js --outdir=dist --bundle --watch --sourcemap --loader:.woff2=copy --loader:.woff=copy --loader:.eot=copy --loader:.ttf=copy --loader:.svg=dataurl",
    "build": "esbuild recursos/js/ingreso.js --outdir=dist --bundle --minify --loader:.woff2=copy --loader:.woff=copy --loader:.eot=copy --loader:.ttf=copy --loader:.svg=dataurl"
  }
}
